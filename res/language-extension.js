/**
 * Package comment.
 */
$package("js.net");

$import("js.net");

$extends(js.net.RPC, js.net.XHR);

$implements(js.net.URL, js.net.Address);

$augments(target, source);

$arguments(arguments, 1);

$eval("scripting code");

$id("element-id");
